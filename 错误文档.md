# opencode serve + cli-wechat-bridge 调通记录

## 问题现象

`opencode serve` 被 cli-wechat-bridge 的 companion 子进程调用时：
- TCP 端口短暂打开（`waitForTcpPort` 成功）→ 随即关闭
- Health check 得到 `ECONNREFUSED`
- 进程存活但 stdout/stderr 完全为空
- 无任何错误信息

## 三层根因

| # | 根因 | 触发条件 | 修复 |
|---|------|----------|------|
| 1 | **Bun pipe 模式下 module loading 期间的 unhandledRejection 导致进程静默挂起** | `stdio: ['ignore', 'pipe', 'pipe']` 即非 TTY 模式 | `serve.ts` 加 `process.on("unhandledRejection", () => {})` |
| 2 | **Bun `--compile` 生成的 binary 对 `reserveLocalPort` 释放的端口有绑定 race** | `reserveLocalPort()` 关闭后立即 `server.listen()` 到同一端口 | 放弃 binary，使用 `bun run` 源码版 |
| 3 | **Companion 的 `checkHealth()` 只做一次，server Effect layer 构建未完成** | Server 启动 < 1s 内 health check | 加 retry 循环（最多 10 次 × 500ms） |

## 次要因素

| 因素 | 说明 |
|------|------|
| **NO_PROXY 缺失** | raw `process.env` 可能没设 `NO_PROXY`，导致 fetch 走代理失败。改用 `buildCliEnvironment()` |
| **early health handler** | 在 raw `http.Server` 上加早期响应器，health check 不用等 Effect layer 构建完 |

## 关键改动文件

### 1. `packages/opencode/src/server/server.ts`

```typescript
// serverLayer() 中加入：在 createServer() 之后、server.listen() 之前
// 注册早期 health handler，自动在真实路由注册时移除
const healthHandler = (req, res) => {
  if (req.method === "GET" && (req.url === "/session/status" || req.url === "/health")) {
    res.writeHead(200, { "content-type": "application/json" })
    res.end("{}")
  }
}
server.on("request", healthHandler)
// addListener 覆写：真实 listener 注册时自动移除 healthHandler
```

### 2. `packages/opencode/src/cli/cmd/serve.ts`

```typescript
// 加在模块顶层，import 之后、export 之前
process.on("unhandledRejection", () => {})
```

### 3. `.nvm/.../cli-wechat-bridge/dist/bridge/bridge-adapters.opencode.js`

```typescript
// startServerProcess(): env = process.env → env = buildCliEnvironment(this.options.kind)
// checkHealth(): 一次性 fetch → 最多 10 次 × 500ms 重试
```

### 4. `~/.local/bin/opencode-dev`

```bash
#!/bin/bash
cd ~/Project/opencode-fix
exec bun run --cwd packages/opencode --conditions=browser src/index.ts "$@"
```

### 5. `~/.local/bin/opencode`

Version 1.17.9 编译 binary（备用，当前未使用）

## 最终架构

```
微信 → cli-wechat-bridge (bridge, Node.js)
     → companion (Node.js → 子进程)
         → opencode (bun run --conditions=browser, 源码版)
             → serve 命令 (process.on("unhandledRejection") 修复)
                 → server.listen() (early health handler)
                     → Effect layer 构建 (含 retry 等待)
```

## 待办

- [ ] 初始化 git 并 commit 当前改动
- [ ] 找到 `process.on("unhandledRejection")` 的根本来源（是哪个模块抛的 rejection），修掉它而不是加 noop handler
- [ ] 确认 binary 的端口 race 是否是 Bun 的 bug，如果是考虑 upstream 报告
- [ ] 把 bridge-adapters 的修改整理成 PR 提交给 cli-wechat-bridge 上游

## 调试时间线

1. 初始状态：companion → `fetch failed`
2. 加 early health handler → 仍然失败
3. 加文件日志 `appendFileSync` → 发现 server 模块顶层代码未执行
4. 加 `process.on("uncaughtException")` → 修复了 `bun run` 版
5. 缩小范围：去掉 `uncaughtException` 保留 `unhandledRejection` → 仍然 work
6. 确认根因：unhandled promise rejection 在 Bun pipe 模式下挂起
7. 发现 binary 版有独立问题：`reserveLocalPort` 端口 race
8. 最终方案：`opencode-dev` 指向源码版 + serve.ts 加 handler
9. 端到端验证通过 ✅
